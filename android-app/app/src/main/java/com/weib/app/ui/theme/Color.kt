package com.weib.app.ui.theme

import androidx.compose.ui.graphics.Color

object WeibPalette {
    const val Title: Long = 0xFF0F172A
    const val Body: Long = 0xFF334155
    const val Muted: Long = 0xFF64748B
    const val Background: Long = 0xFFF5F7FB
    const val Surface: Long = 0xFFFFFFFF
    const val Primary: Long = 0xFF0F766E
    const val Accent: Long = 0xFF00A896
    const val LightAccent: Long = 0xFFECFEFF
    const val Border: Long = 0xFFE2E8F0
}

val WeibTitle = Color(WeibPalette.Title)
val WeibBody = Color(WeibPalette.Body)
val WeibMuted = Color(WeibPalette.Muted)
val WeibBackground = Color(WeibPalette.Background)
val WeibSurface = Color(WeibPalette.Surface)
val WeibPrimary = Color(WeibPalette.Primary)
val WeibAccent = Color(WeibPalette.Accent)
val WeibLightAccent = Color(WeibPalette.LightAccent)
val WeibBorder = Color(WeibPalette.Border)
